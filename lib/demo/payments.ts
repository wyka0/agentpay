import type { Payment } from "@/types";

/**
 * No payments have been made. Arc payment execution is not implemented in
 * Sprint 0, so this list stays empty — never seeded with sample transactions.
 */
export const DEMO_PAYMENTS: readonly Payment[] = [];
